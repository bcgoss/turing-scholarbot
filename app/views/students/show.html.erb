<h1><%= @student.name %></h1>

<%= link_to "Add Course", new_student_enrollment_path(@student) %>
<ul class="current-courses">
  <% @student.enrollments.each do |enrollment| %>
    <li><%= enrollment.subject %>: <%= enrollment.score %></li>
  <% end %>
</ul>